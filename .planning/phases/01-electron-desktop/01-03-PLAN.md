---
phase: 01-electron-desktop
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified: []
autonomous: false
requirements:
  - ELEC-01
  - ELEC-02
  - ELEC-03
  - ELEC-04
  - ELEC-05
  - PDF-04
  - DATA-02
  - DATA-03
  - DATA-04
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - TXN-01
  - TXN-02
  - TXN-03
  - TXN-04
  - GOAL-01
  - GOAL-02
  - GOAL-03
  - GOAL-06
  - RITUAL-01
  - RITUAL-02
  - RITUAL-03
  - RITUAL-04
  - RITUAL-05
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "App launches from double-click (or npm run dev) and shows the React UI in an Electron window"
    - "Setup wizard appears on first run with no prior localStorage data"
    - "All 6 dashboard category bars update when a transaction is added"
    - "Weekly ritual completes all 5 steps and the score appears on the dashboard"
    - "Goal cards, history charts, and PDF import all function inside the Electron window"
    - "Closing and reopening the app shows the same state (localStorage persists)"
    - "No outbound network requests are made at runtime"
    - "Corrupted localStorage causes fallback to setup wizard, not a crash"
    - "pdfjs worker resolves without console errors when PDF import is used"
    - "BrowserWindow has contextIsolation: true and nodeIntegration: false confirmed"
  artifacts:
    - path: "out/main/index.cjs"
      provides: "Compiled Electron main process"
    - path: "out/preload/index.cjs"
      provides: "Compiled preload contextBridge"
    - path: "out/renderer/index.html"
      provides: "Compiled renderer bundle"
  key_links:
    - from: "Electron window"
      to: "React app"
      via: "loadURL (dev) / loadFile (prod)"
      pattern: "loadURL|loadFile"
---

<objective>
Verify that Phase 1 is complete: the Electron app launches, all pre-existing features function identically to the web build, localStorage persists across restarts, the pdfjs worker resolves cleanly, and the three smart features from Plan 02 (goal weekly calc, overspending notifications, folder watcher UI) are visible and functional.

Purpose: Catch integration failures before declaring Phase 1 done. This is a verification-only plan — no code is written except for any targeted fixes discovered during verification.

Output: Confirmation that all Phase 1 success criteria are met, or a list of defects found with targeted fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-electron-desktop/01-01-SUMMARY.md
@.planning/phases/01-electron-desktop/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — compile check and static analysis</name>
  <files></files>
  <read_first>
    - /home/shrijit/Projects/budgeting_app/package.json — confirm scripts and "main" field
    - /home/shrijit/Projects/budgeting_app/electron.vite.config.js — confirm renderer.root and plugins
    - /home/shrijit/Projects/budgeting_app/src/main/index.js — confirm contextIsolation, nodeIntegration, sandbox values
    - /home/shrijit/Projects/budgeting_app/src/utils/pdfParser.js — confirm workerSrc line
  </read_first>
  <action>
Run the following checks in sequence. Report pass/fail for each. Fix any failures before proceeding to Task 2.

Check 1 — Build compiles without errors:
```bash
cd /home/shrijit/Projects/budgeting_app && npm run build
```
Expected: exits 0, no TypeScript or Rollup errors.
If build fails: read the error, fix the root cause (likely an import path or syntax error), re-run.

Check 2 — Output files exist:
```bash
ls out/main/index.cjs out/preload/index.cjs out/renderer/index.html
```
Expected: all three files exist.

Check 3 — Security config in compiled main:
```bash
grep -n "contextIsolation" out/main/index.cjs
grep -n "nodeIntegration" out/main/index.cjs
```
Expected: contextIsolation appears with value true, nodeIntegration appears with value false.

Check 4 — pdfjs workerSrc is correct:
```bash
grep -n "workerSrc" src/utils/pdfParser.js
```
Expected: single line `mod.GlobalWorkerOptions.workerSrc = './pdf.worker.min.mjs'` — no import.meta.url.

Check 5 — public/pdf.worker.min.mjs exists:
```bash
ls public/pdf.worker.min.mjs
```
Expected: file exists.

Check 6 — getWeeklyRequired exported:
```bash
grep -n "export function getWeeklyRequired" src/utils/recommendations.js
```
Expected: function exported.

Check 7 — Notification guard in AddTransaction:
```bash
grep -n "electronAPI?.showNotification\|electronAPI?\.showNotification" src/components/AddTransaction.jsx
```
Expected: optional chaining present.

Check 8 — FolderWatcherRow hides in non-Electron mode:
```bash
grep -n "window.electronAPI" src/components/Dashboard.jsx | head -5
```
Expected: `if (!window.electronAPI) return null` present.

Fix any failures. If Check 1 (build) fails, investigate and fix before running other checks.
  </action>
  <verify>
    <automated>
      cd /home/shrijit/Projects/budgeting_app && npm run build 2>&1 | tail -5 && echo "---BUILD EXIT: $?"
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - out/main/index.cjs exists
    - out/preload/index.cjs exists
    - out/renderer/index.html exists
    - src/utils/pdfParser.js workerSrc uses './pdf.worker.min.mjs' (no import.meta.url)
    - public/pdf.worker.min.mjs exists
    - src/components/AddTransaction.jsx uses optional chaining `?.showNotification`
  </acceptance_criteria>
  <done>Build compiles clean, all static checks pass, any discovered issues fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 installed electron-vite, created the Electron main process (src/main/index.js) with BrowserWindow at 480px, contextIsolation: true, nodeIntegration: false, and the chokidar folder watcher IPC handlers. Created the preload contextBridge (src/preload/index.js) exposing window.electronAPI. Fixed pdfParser.js to use static './pdf.worker.min.mjs'.

    Plan 02 added: (a) goal weekly savings calculation in GoalCard ("Save $X.XX/wk to reach by [date]" with green/amber indicator), (b) native overspending notification in AddTransaction.jsx via window.electronAPI?.showNotification, (c) folder watcher settings UI in Dashboard.jsx, (d) IPC-triggered PDF import path in PDFImporter.jsx.

    Task 1 of this plan confirmed the build compiles clean.
  </what-built>
  <how-to-verify>
Run `npm run dev` and perform these checks in order:

**App launch:**
1. Electron window opens at approximately 480px wide with the dark glassmorphism UI
2. DevTools opens automatically (dev mode behavior)

**First-run setup wizard (if no prior data):**
3. Setup wizard appears asking for weekly income — enter a number and proceed through all 3 steps
4. After completing setup, dashboard appears

**Dashboard (DASH-01 through DASH-04):**
5. Score ring is visible with a 0-100 number
6. Six category bars are visible (Housing/Food/Transport/Health/Entertainment/Savings — or the app's exact 6 categories)
7. Add a transaction via the + button: enter amount, description, date, category — click Save
8. Category bar for that category updates immediately without page reload

**Transactions (TXN-01 through TXN-04):**
9. Manual transaction appears in the recent transactions list with date, description, amount, category
10. Edit or delete the transaction — confirm it works
11. Category of a transaction can be changed via dropdown

**Goals (GOAL-01 through GOAL-03, GOAL-06):**
12. Navigate to Goals tab — create a goal with a name, target amount, and a target date ~10 weeks from today
13. Verify goal card shows "Save $X.XX/wk to reach by [date]" in amber or green color
14. Click "Add funds" and add a partial amount — verify the progress bar updates
15. Delete a goal — verify it asks for confirmation (or disappears with one click as per existing behavior) and is removed

**Weekly ritual (RITUAL-01 through RITUAL-05):**
16. Navigate to the ritual tab and complete all 5 steps
17. After completion, score appears on the dashboard

**History (HIST-01 through HIST-04):**
18. Navigate to History tab — verify line chart and savings chart are visible (may show sparse data with only 1-2 weeks)

**PDF import (PDF-04):**
19. Navigate to Goals (or wherever PDF import button is) and click "Import"
20. Drop a bank statement PDF — verify transactions are extracted and review modal appears (no console errors about worker)

**Data persistence (DATA-01 through DATA-04):**
21. Close the Electron window completely and reopen with `npm run dev`
22. Verify the same state (transactions, goals, score, streak) is shown — data survived restart
23. Open browser DevTools → Application → Local Storage — confirm key `budgetpulse_v1` exists with data
24. To test DATA-04: in DevTools console run `localStorage.setItem('budgetpulse_v1', 'CORRUPTED')` then reload — verify app shows setup wizard, not a crash

**Smart features (from Plan 02):**
25. Add a transaction that exceeds a category's weekly budget — check OS notification center for "Over budget: [category]" notification
26. In Dashboard, scroll to bottom — verify "Watch folder" row is visible with a "Select" button; click it to confirm the system folder picker dialog opens

**Security (ELEC-02, ELEC-03):**
27. In DevTools console, run `require('fs')` — expected: `Uncaught ReferenceError: require is not defined` (nodeIntegration is false)
28. Run `window.electronAPI` — expected: object with pickFolder, watchFolder, showNotification etc.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe exactly which step failed and what was observed — the executor will fix the issue and ask you to re-verify that specific item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification only | No new code paths introduced in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | renderer DevTools console | accept | nodeIntegration: false verified in step 27; renderer cannot call require() or access Node APIs directly |
| T-03-02 | Information Disclosure | DevTools open in dev mode | accept | DevTools open only when !app.isPackaged; packaged builds do not expose DevTools |
</threat_model>

<verification>
All verification is performed in the checkpoint task above. The human-verify checkpoint is the gate for declaring Phase 1 complete.

After human approval, mark all 35 Phase 1 requirement IDs as complete in REQUIREMENTS.md traceability table:
SETUP-01, SETUP-02, SETUP-03, SETUP-04, DASH-01, DASH-02, DASH-03, DASH-04, TXN-01, TXN-02, TXN-03, TXN-04, GOAL-01, GOAL-02, GOAL-03, GOAL-06, RITUAL-01, RITUAL-02, RITUAL-03, RITUAL-04, RITUAL-05, HIST-01, HIST-02, HIST-03, HIST-04, DATA-01, DATA-02, DATA-03, DATA-04, ELEC-01, ELEC-02, ELEC-03, ELEC-04, ELEC-05, PDF-04
</verification>

<success_criteria>
1. `npm run build` exits 0 with all three output files present
2. Human verifier confirms all 28 manual verification steps pass
3. `require('fs')` in renderer DevTools returns ReferenceError (nodeIntegration: false confirmed)
4. localStorage key `budgetpulse_v1` persists across app restart
5. Corrupted localStorage causes setup wizard to appear (not a blank screen or JS error)
6. pdfjs worker resolves without console errors when PDF import is used
</success_criteria>

<output>
After human approval and all fixes applied, create `.planning/phases/01-electron-desktop/01-03-SUMMARY.md` following the summary template at @$HOME/.claude/get-shit-done/templates/summary.md

Also update REQUIREMENTS.md traceability table: change Status from "Pending" to "Complete" for all Phase 1 requirement IDs listed in the verification section above.
</output>
